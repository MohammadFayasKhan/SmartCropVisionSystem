# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false
CORS_ORIGINS=*

# Compute Device Target (options: auto, mps, cuda, cpu)
DEVICE_TARGET=auto

# Physical IoT Microcontroller Configuration (for esp8266_firmware.ino)
WIFI_SSID=YOUR_WIFI_SSID
WIFI_PASSWORD=YOUR_WIFI_PASSWORD
BACKEND_HOST_IP=192.168.1.100
BACKEND_PORT=8000
